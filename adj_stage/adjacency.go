package adj_stage

func RunAdjacencyStage(nodeCount uint32, configFile string) {
	//Read each file and assign the labels for each
	//column. Then accumulate edges in files.
	//Then, check if files are small enough, else
	//break them down again and delete the older ones
}
